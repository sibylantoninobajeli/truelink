/*import "package:graphql_flutter/graphql_flutter.dart";
import 'package:truelink/main.dart';
import "package:truelink/services/queryMutationUser.dart";

import 'package:truelink/models/user.dart';
import 'package:truelink/globals.dart' as globals;
*/
abstract class RegistrationScreenContract {
  /*void onRegistrationSuccess(Isatutenze user);
  void onRegistrationError(String errorTxt);*/
}

class RegistrationScreenPresenter {
  //RegistrationScreenContract _view;
  //RestApiDatasource api = new RestApiDatasource();
  //RegistrationScreenPresenter(this._view);

  /*
  QueryMutationUser queryMutation = QueryMutationUser();
  GraphQLClient _client = graphQLConfiguration.clientToAnonQuery();

  doRegister(Isatutenze user,{bool gmailVerified=false}) async{
    QueryResult result = await _client.query(
      QueryOptions(
        //documentNode: gql(queryMutation.getLoginToken("pippino", "123456!"))
          documentNode: gql(
              queryMutation.addUser('', user.user.firstName,  user.user.lastName, user.user.email))
      ),
    );
  }
  */

}